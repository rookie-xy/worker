package register
/*
import "github.com/rookie-xy/worker/src/output"

func Output(name string, f output.Factory) {
    name = name[strings.LastIndex(name, ".") + 1:]
    if name == "" {
        return
    }

    if _, exists := output.Ouputs[name]; exists {
        panic(fmt.Sprintf("codec '%v' already registered ", name))
    }

    output.Ouputs[name] = f
}
*/
